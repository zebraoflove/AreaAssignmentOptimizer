# PROJECT STATE

## Текущее состояние

Проект находится на этапе **нормализации территориальных данных**.

Базовая инфраструктура, генерация кандидатов, алгоритм `Greedy` и экспериментальная инфраструктура реализованы.

## Реализовано

- архитектура и структура проекта;
- SQLite и база `world.db`;
- импорт и подготовка исходных данных;
- генерация `candidate sets`;
- базовый алгоритм `Greedy`;
- сохранение результатов сопоставления;
- анализ качества назначений;
- экспериментальная инфраструктура;
- экспорт результатов в Excel;
- удаление устаревших модулей первоначальной архитектуры.

## Текущий baseline

`Greedy` используется как базовый алгоритм для последующего сравнения.

Текущая реализация поддерживает:

- ограничение `country area <= subject area`;
- порядок обработки `original`;
- порядок обработки `ascending`;
- порядок обработки `descending`.

## Текущее ограничение

Состав государств и территорий ещё не приведён в соответствие с окончательной постановкой задачи.

Необходимо определить правила:

- включения государств-кандидатов;
- объединения зависимых территорий с государствами, от которых они зависят;
- обработки спорных территорий;
- учёта Антарктиды;
- обеспечения однократного учёта учитываемой суши.

## Следующий этап

**Нормализация территориальных данных.**

После завершения нормализации необходимо повторно оценить `Greedy` на обновлённом наборе государств-кандидатов.